# Start tmux directly
if command -v tmux &> /dev/null && [ -n "$PS1" ] && [[ ! "$TERM" =~ screen ]] && [[ ! "$TERM" =~ tmux ]] && [ -z "$TMUX" ]; then
  exec tmux
fi

# Add SSH keys to the session
ssh-start() {
  eval `ssh-agent`
  ssh-add ~/.ssh/github
}

ssh-stop() {
  kill $SSH_AGENT_PID
}

